---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { featuredProjects } from "../data/projects";
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-5xl font-bold">강민수 · Minsu Kang</div>
    <div class="text-3xl py-3 font-bold">데이터로 문제를 풀고, 서비스로 만듭니다.</div>
    <div class="py-2">
      <p class="text-lg">
        데이터 분석·예측 모델링·서비스 구현을 실제 문제에 연결합니다.<br />
        교통·해양·법률 등 여러 도메인의 문제를 <b>데이터로 풀어왔습니다.</b><br />
        최근에는 <b>대회·해커톤 기획·운영</b>과 <b>AI 시스템 개발</b>을 함께 다루고 있습니다.
      </p>
    </div>
    <div class="mt-8 flex flex-wrap gap-3">
      <a class="btn btn-outline" href="/projects">Projects</a>
      <a class="btn btn-outline" href="/cv">CV</a>
      <a
        class="btn btn-outline gap-1"
        href="https://github.com/Minsu5452"
        target="_blank"
        rel="noopener noreferrer"
      >
        GitHub
        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M7 17L17 7"/><path d="M7 7h10v10"/>
        </svg>
      </a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">대표 프로젝트</div>
    <p class="text-sm opacity-70 mb-5">카드를 누르면 프로젝트 상세 페이지로 이동합니다.</p>
  </div>

  <div class="grid gap-4">
    {
      featuredProjects.map((p) => (
        <HorizontalCard
          title={p.title}
          desc={p.summary}
          url={`/projects/${p.slug}/`}
          badge={p.badge}
          hero={p.hero}
          color={p.color}
        />
      ))
    }
  </div>

  <div class="mt-10 text-center">
    <a class="btn btn-outline" href="/projects">전체 프로젝트 보기 →</a>
  </div>
</BaseLayout>
